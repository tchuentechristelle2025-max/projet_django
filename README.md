creer un dossier vide
creer un fichier 
     -requierements.txt

ecrire DJANGO dans le requirements.txt

installer les dependances
      - pip install -r requirements

creer le projet django 
     -django-admin startprojet blog .

creer le composant:
     - python manage.py start app articles

enregistrer les migrations 
     -python manage.py migrate

creer un admin python 
    -manage.py createsuperuser

demarrer le serveur
   - python manage.py runserver